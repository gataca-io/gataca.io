'use strict';

/**
 * credential-example router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::credential-example.credential-example');

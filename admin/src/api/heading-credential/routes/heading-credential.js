'use strict';

/**
 * heading-credential router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::heading-credential.heading-credential');
